# Bentas Design System — Proje Kuralları

## CSS Değişkenleri (Design Tokens) — ZORUNLU

`docs/css/styles.css` kapsamlı bir `--bt-*` design token seti tanımlıyor: spacing, radius, renk/surface, tipografi (font-size + line-height dahil). Yeni bir component eklerken veya mevcut bir component'i değiştirirken **her görsel değer** ilgili token'dan gelmeli — hardcoded px/hex yazılmamalı.

- **Spacing (padding, margin, gap):** `var(--bt-space-{none|2xs|xs|sm|md|lg|xl|2xl|3xl...}, fallback-px)`
- **Font-size / line-height:** `var(--bt-text-{2xs|xs|sm|md|lg|xl|2xl...}-size)` ve `var(--bt-text-{...}-lh)` — asla elle `font-size:12px` / `line-height:16px` yazma
- **Renkler (background, color, border-color):** `var(--bt-surface-*)`, `var(--bt-text-*)`, `var(--bt-border-*)` — hex kodunu sadece fallback olarak yaz: `var(--bt-token, #hex)`
- **Border-radius:** `var(--bt-radius-{none|xs|sm|md|lg|xl...})`

Sabit piksel değeri sadece **token karşılığı olmayan** boyutlarda kabul edilebilir (örn. bir ikonun tam 16×16px olması gibi element-özel bir ölçüm — bu da önce Figma'dan doğrulanmalı).

## Yeni token gerekiyorsa

Önce `docs/css/styles.css` `:root` bloğundaki mevcut `--bt-*` token'lar arasında eşleşen bir değer ara. Yoksa Figma'daki gerçek variable adını/değerini kullanıcıya danışmadan icat etme: Figma Desktop Bridge ile ilgili node'u incele (`get_metadata` → `get_design_context` → `get_screenshot`), çıkan `var(--.../token-name, fallback)` değerini styles.css'teki karşılığıyla eşleştir.

## Kontrol (component tamamlandığında)

O component'in CSS bloğunda hardcoded px/hex değer kalmadığından emin ol — yalnızca `var(--bt-*, fallback)` deseninin fallback kısmında px/hex görünmeli. CSS selector specificity'ye dikkat: aynı elementi hedefleyen daha genel bir kural (örn. `.content p`) component'in kendi class'ını ezebilir — component class'larını yeterince spesifik yaz (örn. `.bt-x-field .bt-x__desc`).

## Mevcut Component'leri Reuse Et — ZORUNLU

Yeni bir component oluştururken, içinde kullanılan alt öğeler (buton, ikon, input vb.) için **özel CSS yazmak yerine tasarım sistemine eklenmiş component class'larını kullan**.

- **Button**: `bt-btn bt-btn--{size} bt-btn--{variant}` — asla custom `button` stili yazma
- **Genel kural**: O öğenin design system'de karşılığı varsa onu kullan; yoksa yeni component olarak ekle

Örnek (Upload → Select Files):
```html
<!-- ✗ Yanlış: custom class -->
<button class="bt-dropzone__link">Select Files</button>

<!-- ✓ Doğru: gerçek button component -->
<button class="bt-btn bt-btn--xs bt-btn--primary-ghost">Select Files</button>
```

Yeni component tasarlanırken Figma'da iç öğelerin hangi design system component'ini kullandığı `get_design_context` çıktısından (`data-name="Button"` gibi) anlaşılabilir — o component'in class'larını kullan.

## Component İsimlendirme Kuralı

Bileşen adları **PascalCase** olmalı, kelimeler ayrı harf büyüklüğüyle birleştirilmeli:
- `SearchBox` (✓) — `Searchbox` değil (✗)
- `SplitButton` (✓), `IconButton` (✓), `TextBox` (✓)

Figma sayfa adı referans alınır; belirsizlik varsa major design system'lerdeki (Material, Carbon, Fluent) yaygın kullanım tercih edilir.

## Isolation Mode (Playground Toolbar)

`docs/js/playground.js` içindeki isolation butonu **tüm playground'larda standarttır** — ekstra kurulum gerekmez.

Yeni bir component eklendiğinde sadece `registerPlayground({id: 'pgd-xxx-overview', ...})` yeterli. `isolation.html`, `window.PAGES_WEB` üzerinden tüm sayfaların `render()` fonksiyonlarını çağırarak `_pgdConfigs`'i doldurur ve ilgili playground'ı `pgd_id` URL parametresiyle bulur.

`window.PGD_ISOLATE` kaydı veya `config.isolate` **artık yeni componentler için gerekli değil** — sadece Sidebar ve Alert gibi eskiden eklenmiş özel mount davranışları olan componentlerde kalır.

## Playground Preview Centering — ZORUNLU

`registerPlayground`'daki `preview` callback'i döndürdüğü HTML'i her zaman bir centering wrapper'a sarmalıdır — aksi hâlde component playground'da sola yaslanır.

İki standart kalıp:

```javascript
// Küçük / buton / ikon tipi bileşenler:
return `<div style="display:flex;align-items:center;justify-content:center;padding:16px;">${html}</div>`;

// Form / full-width bileşenler:
return `<div style="max-width:440px;width:100%;margin:0 auto;">${html}</div>`;
```

Yeni bir component eklenirken bu wrapper **zorunludur** — eklenmezse playground'da sola yaslanır.

## Playground CSS Sekmesi — ZORUNLU

`registerPlayground` çağrısında **`css` callback'i her zaman eklenmelidir** — bu, playground'daki Preview / Code / **CSS** üç sekmesinin tamamının görünmesini sağlar.

```javascript
registerPlayground({
  id: 'pgd-xxx-overview',
  variants: VARIANTS,
  preview: (state, p) => xxxHtml(state, p),
  code:    (state, p) => xxxHtml(state, p),
  css:     (state, p) => xxxCss(state, p),   // ← ZORUNLU
})
```

`xxxCss(state, props)` fonksiyonu:
- Seçili state/props kombinasyonuna göre **ilgili CSS kurallarını** üretir
- Her property değerinin yanına token adı + fallback hex comment ekler: `var(--bt-text-brand-default)  /* #0d4e97 */`
- Dosya başında top-level `const`/`function` olarak tanımlanır (render closure içinde değil)
- Satır üretimi için `const p = (k, v) => \`  \${k}: \${v};\`` helper pattern kullanılır
- **Return formatı ZORUNLU** — diğer componentlerle aynı `<pre>` wrapper ve HTML escape:
```javascript
const esc = s => s.replace(/&/g, '&amp;').replace(/</g, '&lt;').replace(/>/g, '&gt;');
return `<pre class="code-block" style="margin:0;border-radius:0;border:none;min-height:100%;">${esc(lines.join('\n'))}</pre>`;
```

## design.md ve CLAUDE.md senkronizasyonu — ZORUNLU

Bu projede oluşturulan component'ler (markup + CSS + JS davranışı) **bundan sonraki
Bentaş projelerinde de kullanılacak** — yani bu repo sadece kendi docs sitesi değil,
aynı zamanda diğer projelerin kopyalayıp yapıştıracağı bir referans kaynağı.
Bu yüzden bir component eklendiğinde/değiştirildiğinde:

1. `docs/css/styles.css` + `docs/js/pages-web.js` (gerçek implementasyon) güncellenir.
2. **`design.md`** o component'in ilgili bölümü (yapı + CSS + JS davranışı, taşınabilir/token-adı-agnostik anlatımla) güncellenir — yeni bir projede bu dosya tek başına yeterli olmalı.
3. **`CLAUDE.md`**'nin "Geçmiş" bölümüne kısa bir not eklenir.

Bunu component değişikliği yapılan HER oturumda otomatik yap, kullanıcı ayrıca hatırlatmasın.

- **2026-07-27 (devam 18)**: "left icon no default gelsin" isteğiyle Accordion Overview playground'undaki "Left Icon" toggle'ının varsayılanı tekrar Yes'ten No'ya çevrildi (`default: 'no'` — devam 15'te tam tersi istenmişti, kullanıcı fikrini değiştirdi) — bu artık component'in kendi `showLeftIcon = false` varsayılanıyla (gerçek Figma "With Content" kullanımı) tutarlı.
- **2026-07-27 (devam 17)**: Kullanıcı 3 küçük düzeltme istedi: (1) Toolbar'daki Description/Left Icon toggle'larının Show/Hide etiketleri Yes/No'ya çevrildi — `ACC_DESC_OPTS`/`ACC_LEFT_OPTS` artık ayrı tanımlanmıyor, proje genelindeki `TBX_BOOL_OPTS` referansı yeniden kullanılıyor (`accPreview`/`accCode`'daki `!== 'hide'`/`=== 'show'` kontrolleri `!== 'no'`'ya çevrildi, ikisinin de default'u 'yes'). Bu sırada devam 15'te atlanan bir bug da düzeltildi: playground `props` config'indeki Left Icon `default` değeri hâlâ `'hide'` yazıyordu (sadece `ACC_LEFT_OPTS` dizisinin sırası değişmişti, asıl default hiç değişmemişti) — şimdi gerçekten `'yes'`. (2) Accordion content grid'indeki her TextBox hücresine label eklendi (`.bt-tbx__meta > .bt-tbx__label`, "Label Text"). (3) Grid hücreleri arası gap `--bt-space-md`'den `--bt-space-2xl`'e (24px) çıkarıldı.
- **2026-07-27 (devam 16)**: Accordion'un playground content örneğine (metnin altına) kullanıcı isteğiyle TextBox bileşeninin md/readonly halinden 2x2 (2 yatay x 2 dikey) grid eklendi — mevcut TextBox CSS/HTML'i (`_tbxCls`, `_tbxInputInner`) sıfırdan yazmadan yeniden kullanılarak `_accTbxCell()`/`accTbxGridHtml()` yardımcıları eklendi (`_accContentWithGrid`, `accExampleItems`'te `_accContentText` yerine kullanılıyor).
- **2026-07-27 (devam 15)**: "left icon show default gelsin" isteğiyle Accordion Overview playground'unda "Left Icon" toggle'ının varsayılanı Hide'dan Show'a çevrildi (`ACC_LEFT_OPTS` sıralaması da Show önce gelecek şekilde güncellendi) — bileşenin kendi component-level varsayılanı (`showLeftIcon = false`, gerçek Figma "With Content" kullanımıyla tutarlı) değişmedi, sadece playground'un keşfedilebilirlik için gösterdiği varsayılan değişti.
- **2026-07-27 (devam 14)**: Kullanıcı iki şey fark etti/istedi: (1) Kapalıyken border'lar üst üste biniyordu — gerçek bug: `.bt-accordion__content`'in `border-bottom`'u BASE kuralda duruyordu, `grid-template-rows:0fr` sadece yüksekliği sıfırlıyor, border box-model'in parçası olduğu için kapalıyken de görünmeye devam edip header'ın kendi border-bottom'ıyla aynı noktada üst üste biniyordu. Düzeltme: `border-bottom` artık SADECE `.is-open`'da uygulanıyor (`:last-child` kuralı da `.is-open:last-child`'a taşındı). (2) Figma'da olan ama "With Content" örneklerinde kapalı olduğu için hiç eklenmemiş **Left Control** (sol ikon slotu) kullanıcı isteğiyle geri getirildi — playground'a "Left Icon" (Show/Hide) toggle'ı eklendi (`ACC_LEFT_OPTS`, varsayılan Hide — gerçek kullanım davranışıyla tutarlı). İkon için Figma'nın "Icon/placeholder" asset'i `curl` ile indirilip incelendi — lucide "scan" ikonu olduğu doğrulandı (eski `pages-mobile.js` implementasyonu da aynı ikonu zaten kullanıyormuş, `iScan`), path birebir alındı, rengi `--bt-icon-primary-strong` (#535353) semantik token'ına bağlandı.
- **2026-07-27 (devam 13)**: Accordion'a kullanıcı isteğiyle Figma'nın dışında 3 özelleştirme eklendi (§14.4 olarak design.md'ye işlendi, bilinçli sapmalar): (1) Header padding'i yatay/dikey ayrıştırıldı — dikey `--bt-space-md` kaldı, yatay `--bt-space-xl`'e çıkarıldı; content'in yatay padding'i de header'la hizalı kalması için aynı şekilde xl'e çekildi (Figma'da content `px-md py-xl` idi, artık dört yönde de xl). (2) Hover'da title alt çizili (`text-decoration:underline`). (3) Açılış/kapanış artık `[hidden]`/`display:none` yerine `grid-template-rows: 0fr↔1fr` (250ms ease, `prefers-reduced-motion` ile kapatılabilir) ile smooth animasyonlu — bu, JS ile yükseklik ölçmeye gerek kalmadan değişken uzunluktaki içerikle çalışan bir teknik, HTML yapısı `.bt-accordion__content > .bt-accordion__content-inner > .bt-accordion__content-pad` olarak iç içe genişletildi. Bu sırada ikon mimarisi de sadeleşti: eskiden `btAccToggle` açık/kapalı SVG'yi `innerHTML` ile değiştiriyordu (chevron-down/up, plus/×) — şimdi ikon her zaman "kapalı" glyph'iyle render ediliyor, `.is-active` class'ı CSS `transform:rotate` ile (chevron 180°, plus 45°→×) smooth bir dönüş üretiyor; `_accIconChevronUp`/`_accIconX`/eski `btAccIcon(iconType,isOpen)` imzası kaldırıldı (dead code). `btAccToggle` artık sadece 2 class toggle'lıyor (header `is-active`, content `is-open`), ikona hiç dokunmuyor.
- **2026-07-27 (devam 12)**: Accordion Overview sayfasında birkaç düzenleme, son hali: (1) Playground'a Figma'nın `AccordionBasic`/`AccordionBordered` component'lerindeki `showDescription` prop'unu karşılayan bir **Description (Show/Hide)** toolbar seçeneği eklendi (`ACC_DESC_OPTS`) — aynı Show/Hide kalıbı zaten Checkbox'ta kullanılıyordu. (2) Sayfanın altındaki ayrı statik "Examples" bölümü tamamen kaldırıldı; önce bunun yerine bir "Show Example" toggle butonu denendi, sonra kullanıcı isteğiyle bu da kaldırılıp **playground'un kendisi doğrudan Figma'daki "Basic/Bordered With Content" örneğini gösterecek şekilde** değiştirildi — playground'un zaten var olan Variant seçici (Basic/Bordered) artık hangi "With Content" örneğinin gösterileceğini seçiyor (`accExampleItems`: 4 item, 3.'sü açık), "State" prop'u playground'dan kaldırıldı (tekil state kavramı çok-item'lı bir gruba uymuyordu; State zaten ayrı "States" tablosunda gösteriliyor). `accCode` artık `accPreview` ile aynı `accGroupHtml` çıktısını kullanıyor — Preview/Code sekmeleri tek kaynaktan besleniyor, birbirinden asla sapmıyor.
- **2026-07-27 (devam 11)**: **Accordion** component'i sıfırdan eklendi (`docs/css/styles.css` `.bt-accordion` bloğu + `pages-web.js` `PAGES_WEB['components/accordion']`, tam interaktif — başlığa tıklamak gerçekten açıp kapatıyor, sayfa yeniden render edilmiyor). Figma node `605:30055` — 2 varyant (Basic/Bordered), 2 ikon tipi (Chevron/Plus-X), 5 state — kullanıcının hazırladığı "Accordion Basic With Content" (608:31148) ve "Accordion Bordered With Content" (609:31466) örnekleri dahil incelendi. Figma'nın Bordered için tanımladığı `Position: Single/First/Middle/Last` varyant sistemi **taklit edilmedi** — bunun yerine header/content'ler flat sıralı kardeşler olarak dizilip `:last-child` CSS seçicisiyle kutunun alt kapanışı otomatik sağlandı (hangi item açık olursa olsun doğru çalışıyor, Figma'nın pozisyon-bazlı class mantığından daha sağlam). 3 önemli, ilk bakışta beklenmeyen bulgu: (1) Hover/Active hiçbir state'te arka plan değiştirmiyor — sadece ikon yönü ve (Bordered'da) bir ring; eski `pages-mobile.js` implementasyonu Active'de yanlışlıkla arka plan değişimi varsayıyordu, bu revizyonda düzeltildi. (2) Focus ring opaklığı bu component'e özgü %50 (`rgba(212,212,212,0.5)`) — Button/Split Button'daki %25'ten farklı, Figma'da doğrudan böyle tanımlı, dokunulmadı. (3) Sol ikon slotu (`showLeftControl`) gerçek "With Content" örneklerinde hep kapalı olduğu için koda hiç eklenmedi. `btAccToggle`/`btAccIcon` JS fonksiyonları bir Node mock objesiyle (jsdom yok) açma/kapama/disabled davranışları için ayrıca test edildi. design.md'ye §14 olarak eklendi.
- **2026-07-27 (devam 10)**: Web docs nav'ından (`NAV_WEB`, `pages-web.js`) mobile'a özgü 6 component kaldırıldı: **Bottom Sheet, Bottom Tab Bar, FAB, Snackbar, Toggle, Top App Bar** — hiçbirinin `PAGES_WEB` içinde gerçek implementasyonu yoktu (placeholder'a düşüyorlardı), sadece nav girdisi siliniyor. Bu component'ler `pages-mobile.js`'de (mobile docs) zaten mevcut, web tarafında yerleri yoktu.
- **2026-07-27 (devam 9)**: Kullanıcı "devam 6"daki kararı genişletti: Outline/Flat/Ghost'un **Focus**'unda fill color (arka plan) olmaması kuralı artık **tüm 7 temada, hem Button hem Split Button'da** uygulandı (önceki tur sadece Base/Primary/Secondary + yalnızca Split Button ile sınırlıydı). Değişenler: Button'da `primary/success/warning/error/information`'ın Outline+Flat Focus'undan `background: var(--bt-{tema}-subtle)` kaldırıldı (Base/Secondary zaten doğruydu, Ghost hiç bg almıyordu); Split Button'da aynı 5 temanın Outline+Flat Focus'undan da kaldırıldı (Base/Primary/Secondary zaten "devam 6"da düzeltilmişti). Her iki component'in CSS sekmesi verisi (`_btnCssProps`, `_spltCssProps`) de `focus.bg` alanları silinerek güncellendi — renderer fonksiyonları (`btnCss`/`spltCss`) zaten `if (d.focus.bg)` ile koşullu yazdığı için alan eksikliği hata vermiyor. Artık kural tamamen simetrik: Outline/Flat/Ghost'ta hiçbir temada Focus arka plan almıyor, sadece box-shadow ring görünüyor; arka plan sadece Hover ve Active/Selected'ta beliriyor.
- **2026-07-27 (devam 8)**: Bir önceki notta ("devam 7") ekran görüntüsündeki gri arka planı ".token-table tr:hover" satır hover'ına bağlamıştım — kullanıcı screenshot'ı hover'da almadığını söyleyince bu teori çürüdü. Gerçek kök neden çok daha temeldi: **`.bt-split-btn__button`/`.bt-split-btn__split` hiçbir yerde (ne temel yapı kuralında ne de 21 tema×fill kombinasyonunun hiçbirinde) `background: transparent` set etmiyordu.** Native `<button>` elementleri tarayıcının kendi varsayılan (opak, gri) arka planını taşır; normal Button component'i (`bt-btn`) her outline/flat/ghost kuralında bunu açıkça `background: transparent` ile eziyor, ama Split Button'da bu satır baştan beri hiç yazılmamıştı — `grep "background: transparent"` split bloğunda sıfır sonuç veriyordu. Çözüm: `.bt-split-btn__button, .bt-split-btn__split` temel yapı kuralına `background: transparent;` eklendi (styles.css). Solid fill'in kendi tema-özel arka planı (2 class'lı, daha spesifik selector) bunun üzerine sorunsuz yazmaya devam ediyor. **Ders:** Ekran görüntüsü verilmeden önceki üç round (surface color, focus bg, divider) hepsi doğru yönde ilerlese de asıl kökü bulamamıştı — kullanıcının somut ekran görüntüsü paylaşması, CSS'i "hangi state'te ne olmalı" diye teorik okumak yerine "hangi kural hiç yazılmamış" diye ters yönden aramaya götürdü.
- **2026-07-27 (devam 7)**: Kullanıcı "yine olmadı" diyerek Figma'da somut bir örnek verdi (node `599:30022`, "Examples" — 4 SplitButton: Primary Solid/Outline/Flat/Ghost). Bu sefer bulunan gerçek hata **divider (ayraç) rengiydi**, surface/arka plan değil: Figma'nın gerçek divider SVG asset'lerini `curl` ile indirip inceledim — Primary temada divider, fill mode'dan (Solid/Outline/Flat/Ghost) tamamen bağımsız olarak **hep aynı renk** (`#0E62BB` = `--bt-primary-solid`) kullanılıyor; aynı şekilde Base temada da hep aynı gri (`--bt-border-primary-default`, #d4d4d4). Bizim kodumuzda ise divider rengi yanlışlıkla **fill mode'a göre değişiyordu**: Outline'da (zaten solid olan ama) yanlış ton, Flat/Ghost'ta ise solid yerine soluk/yarı-saydam bir rgba kullanılıyordu — üç fill de kendi içinde tutarsızdı. Düzeltme: her temada Outline/Flat/Ghost'un divider'ı TEK bir sabit solid renge (o temanın zaten Outline'da kullandığı doğru border tonu) eşitlendi; Primary özel olarak `--bt-primary-solid`'e çekildi (diğerleri `--bt-primary-default`/`--bt-border-brand-default` kullanıyordu, Figma asset'i blue-600'ü doğruladı). Solid fill'in divider'ı dokunulmadı (o, renkli arka plana karşı kontrast için ayrı bir mantık — açık/koyu temaya göre beyaz/siyah yarı-saydam çizgi — zaten doğruydu). Base+Primary için asset'ten birebir doğrulandı; Secondary/Success/Warning/Error/Information için aynı iç-tutarlılık kuralına göre genellendi (Figma'da bu 5 tema için ayrı asset kontrol edilmedi — ileride tek tek doğrulanabilir). **Not:** `bt-btn-group` (Button Group) component'i yorumda "mirrors Split Button" diyerek AYNI eski (yanlış) divider şemasını kopyalamış — bu oturumda kapsam dışı bırakıldı, ayrı bir session'da düzeltilmeli.
- **2026-07-27 (devam 6)**: Bir önceki notta ("devam 5") Primary/Secondary Outline&Flat'ın Focus'una Button'daki gibi `background: var(--bt-primary-subtle)` eklenmesi "doğru" kabul edilmişti — kullanıcı bunun asıl istediği şey olmadığını netleştirdi: **Base/Primary/Secondary temalarında Outline/Flat/Ghost'un Default/Focus/Disabled state'lerinde hiç surface color (arka plan) olmamalı** — sadece Hover ve Active/Selected'ta arka plan görünmeli. Bu, kullanıcının açık kararıyla **yalnızca Split Button'da** uygulandı, Button component'i (`bt-btn`) kasıtlı olarak değiştirilmedi (Button'ın kendisi bu üç temada Focus'a hâlâ subtle bg ekliyor — bilinçli bir ayrışma). Denetimde Base/Secondary zaten uyumluydu (daha önceki düzeltmelerden); yalnızca **Primary Outline** ve **Primary Flat**'ın Focus state'inden fazladan `background: var(--bt-primary-subtle)` kaldırıldı (ring/box-shadow aynı kaldı) — Ghost zaten hiçbir zaman arka plan almıyordu (fill mantığı gereği), dokunulmadı. Bu, Split Button'ın artık Button'dan **kasıtlı olarak farklılaştığı** ilk nokta; ileride Button'da da aynı yönde bir değişiklik istenirse ayrıca ele alınmalı.
- **2026-07-27 (devam 5)**: Bir önceki notta ("devam 4") Split Button düzeltmesi yalnızca **Base** temayla sınırlandırılmıştı — kullanıcı "base, success, warning etc hepsi" diyerek bunun eksik olduğunu belirtti. Button'ın **tüm** tema/fill kombinasyonlarını (7 tema × 4 fill × 5 state) satır satır yeniden karşılaştırdım (`grep` ile tam liste çıkarıp elle diff), 4 ek gerçek hata bulundu:
  1. **Secondary Outline · Active/Selected** — Button'da (Base gibi) primary mavisini ödünç alıyor (`--bt-primary-subtle` bg + `--bt-primary-default` inset border); Split Button `--bt-secondary-emphasis` (hover ile aynı) kullanıyordu.
  2. **Secondary Outline · Disabled** — border token'ı Button'da `--bt-border-primary-default` (Base'in istisnasıyla aynı); Split Button `--bt-base-muted` kullanıyordu.
  3. **Secondary Solid · Disabled** — arka plan Button'da kendi tonu `--bt-secondary-muted`; Split Button diğer temalarla aynı `--bt-base-muted`'ı kullanıyordu (Secondary Solid burada tek istisna, diğer 5 tema zaten `--bt-base-muted` kullanıyor ve doğruydu).
  4. **Flat fill · Active/Selected (7 temanın 5'i — Primary/Success/Warning/Error/Information)** — Flat'ın kalıcı border'ı hiç yok (Outline'dan farklı olarak), bu yüzden Button'ın active'te eklediği inset border (kendi tema rengiyle) Split Button'da **hiçbir zaman** uygulanmıyordu, sadece arka plan değişiyordu. Base ve Secondary zaten "devam 4"te / bu oturumda düzeltilmişti; kalan 5 temaya da kendi `--bt-{tema}-default` inset border'ı eklendi.
  `docs/css/styles.css`'te 13 kural güncellendi, `pages-web.js`'teki `_spltCssProps` (CSS sekmesi verisi) aynı 8 fill/tema kombinasyonu için `active.border` alanı ve `secondary-solid.dis.bg` eklenerek güncellendi. Node sandbox'ında 28 tema×fill kombinasyonunun tamamı hatasız render edildi. **Ders:** İlk düzeltme turunda sadece Base temayı örnek alıp genellemiştim — Button'ın "hangi temalar primary'yi ödünç alır" (Base+Secondary) ve "flat'ın active border'ı var mı" (tümü) gibi tema-özel istisnalarını varsaymak yerine her zaman `grep` ile tam listeyi çıkarıp tek tek doğrulamak gerekiyor.
- **2026-07-27 (devam 4)**: **Split Button**'ın Base tema Outline/Flat/Solid state'lerinde Button ile tutarsız surface renkleri bulundu (kullanıcı fark etti) — kök neden: `f515eaf`/`87a6043` commit'leri (14 Temmuz) Button'ın Base tema hatalarını Figma'ya göre düzeltmişti, ama Split Button 2 gün sonra (16 Temmuz, `5b65ef8`) eklenirken bu düzeltmeler hiç yansıtılmamış — sanki düzeltme-öncesi Button koduna bakılarak yazılmış. 3 nokta düzeltildi (`docs/css/styles.css`): (1) Base Outline **Focus**'ta fazladan `background: var(--bt-base-emphasis)` kaldırıldı (Button gibi sadece ring, transparan kalmalı); (2) Base Outline **Disabled** border token'ı `--bt-base-muted` → `--bt-border-primary-default`; (3) Base **Solid/Outline/Flat**'ın **Active & Selected** state'i `background: var(--bt-base-emphasis)` (hover ile aynı) yerine Button'ın çapraz-tema kuralına göre `background: var(--bt-primary-subtle); box-shadow: inset 0 0 0 1px var(--bt-primary-default);`e çevrildi — diğer temalarda (Primary/Secondary/vb.) bu border eklenmiyor çünkü onların wrapper'ı zaten o rengi kalıcı taşıyor (redundant olurdu), ama Base'in wrapper border'ı gri olduğu için bu ekleme görsel olarak anlamlı ve gerekli. `pages-web.js`'teki `_spltCssProps`/`spltCss()` (CSS sekmesi metni) de aynı 3 fill için `active.border` alanı eklenerek güncellendi. Not: CSS sekmesinin focus ring opasitesi (`.50`) ile gerçek CSS'teki (`0.25`) arasında hem Button'da hem Split Button'da zaten var olan, bu düzeltmeden bağımsız küçük bir tutarsızlık fark edildi ama kapsam dışı bırakıldı (ayrı bir oturumda ele alınabilir).
- **2026-07-27 (devam 3)**: **Avatar** component'i sıfırdan, güncel standartlara göre eklendi (`docs/css/styles.css` `.bt-avatar` bloğu + `pages-web.js` `PAGES_WEB['components/avatar']`, playground dahil) — daha önce sadece `pages-mobile.js`'te eski/ayrı bir implementasyon vardı (inline `style=`, kaldırılmış `--bt-text-{size}-size/-lh` token'ları, arka plan/border yanlış, icon'un avatar boyutuyla orantılı büyüdüğü varsayımı — hepsi hatalıydı), o dokunulmadan bırakıldı. Figma node `205:25278`'in 24 varyantının tamamı (6 boyut × 2 tip × 2 tema, gizli olan değil ama tek tek) `get_design_context` ile tek tek sorgulanarak 3 kritik detay bulundu: (1) icon her boyutta **sabit 24×24**, avatar büyüse de büyümüyor; (2) border her iki temada da var (Brand'in koyu mavi arka planında da aynı `--bt-border-primary-default`); (3) icon Figma'nın kendi "Circle-User-Round" fill-tabanlı asset'i — lucide'ın stroke-tabanlı `circle-user-round`'ından farklı, bu yüzden gerçek SVG path'i `curl` ile asset sunucusundan çekilip birebir gömüldü (`fill="var(--fill-0,...)"` → `fill="currentColor"`). Figma'nın raporladığı padding değerleri (`--radius-sm/md/lg/2xl/3xl/4xl` isimli, garip) sabit boyut + 24px icon ile matematiksel olarak tutarsız bulunup (2xs'te 4+4+24=32>24) vestigial/eski auto-layout metadata'sı olarak elendi, kullanılmadı — sadece fixed width/height + flex center uygulandı. Brand arka plan Figma'da literal `blue/700` primitive'ine bağlıydı, projenin semantik-token-öncelik kuralına göre `--bt-surface-brand-default`'a (aynı değer) çevrildi. Node VM sandbox'ında tüm boyut×tema×tip kombinasyonları test edildi. design.md'ye §13 olarak eklendi.
- **2026-07-27 (devam 2)**: External Drop Zone'un durum metni rengi kullanıcı kararıyla Standart Upload ile **aynı** yapıldı — `bt-edz--uploading`/`--completed`/`--failed` modifier class'ları eklendi (`.bt-edz__status` rengini brand/success/error'a çeviriyor), `btUplSetDzState`'in edz dalı artık `zone.className`'i de güncelliyor, `_edzHtml` state modifier class'ını dışarı taşıyor. Not: Figma'nın hidden state frame'lerinde (586:3956/3959/3962) metin rengi nötr görünüyordu (bir önceki oturumda böyle uygulanmıştı) — bu, kullanıcının açık talebiyle bilinçli olarak Figma'dan sapan bir tutarlılık kararı, gözden kaçan bir hata değil. design.md §12.4/§12.5 ve sayfadaki "CSS Properties"/"Usage" metinleri buna göre güncellendi.
- **2026-07-27 (devam)**: External Drop Zone'da default state'in 2 satırlı durum metni ("Drag and drop..." / "( Max. 1KB - 20MB )") yan yana görünüyordu — kullanıcı fark etti. Kök neden: Figma'da (node 586:3954/3955) "Upload Status" konteyneri row-flex, iki `<p>` ise onun İÇİNDE ayrı bir column-flex bloğuna sarılı; implementasyonda bu iç sarmalayıcı atlanıp `<p>`'ler doğrudan row-flex'e konmuştu. `.bt-edz__status-text` (column-flex) sarmalayıcısı eklendi, 4 kod yolu (`btUplSetDzState`, `_edzHtml`, `_edzPreview`, `_edzCode`) tek bir `_edzDefaultStatusHtml` sabitine bağlandı (tekrarı önlemek için). design.md §12.4 örneği ve CSS'i güncellendi.
- **2026-07-27**: Upload'a ikinci tip eklendi: **External Drop Zone** (`.bt-edz`) — Figma node `586:20876` (Base External Drop Zone) + `586:21058` (Default/Single/Multiple segmentleri) Figma Desktop Bridge ile incelendi. Standart Upload'un `Select Files` + tek satır durum metninden farklı olarak: buton üstte, durum metni altta dikey stack; `Select Files` gerçek design system Button'u (`bt-btn bt-btn--sm bt-btn--base-outline`, önceden hiçbir yerde icon+text kombinasyonuyla kullanılmamıştı) + yeni bir plus ikonu (`_uplIconPlus`, lucide `plus`). Upload File (`.bt-upload-file`) ve Upload container (`.bt-upload`) satır satır reuse edildi — Figma'daki file-row yapısı zaten mevcut implementasyonla birebir örtüşüyordu. Gerçek bir hata/yanlış varsayımdan kaçınmak için gizli state frame'leri (`586:3956/3959/3962`) tek tek `get_design_context` ile sorgulandı: Standart Dropzone'un aksine External'da Uploading/Completed/Failed'de **metin rengi hiç değişmiyor** (hep `--bt-text-primary-default`), sadece ikon değişiyor — bu ayrım koda ve design.md'ye işlendi. JS tarafında 6 fonksiyonu (`btUplStartUpload/Remove/Retry/SetDzState`) iki kez yazmak yerine zone lookup'ları `.bt-dropzone, .bt-edz` şeklinde genelleştirildi, `btUplSetDzState` iç dallanmayla her iki kutuyu da destekliyor. Playground `variants` ekseni artık Sidebar'daki Standart/Hub kalıbıyla aynı şekilde **tip** (Standart Upload / External Drop Zone) seçiyor, segment (Default/Single/Multiple) `props` olarak ikinci eksen oldu. Yeni kod bir Node VM sandbox'ında (tarayıcı olmadığı için) tüm tip×segment kombinasyonlarıyla çalıştırılıp doğrulandı. design.md §12 yeniden düzenlendi (§12.4 External Drop Zone, §12.5 JS Davranışı).
- **2026-07-24 (devam 7)**: Figma'nın 5 "Focus Ring" Effect Style'ı (primary/neutral %50, error/success/warning %24 — karışık) kullanıcı kararıyla **hepsi %25 opaklığa** sabitlendi: `rgba(13,78,151,*)`, `rgba(212,212,212,*)`, `rgba(232,75,91,*)`, `rgba(212,175,44,*)` — `styles.css`'te 95, `pages-web.js`'te (canlı state hesaplamaları + dokümantasyon tabloları) 49 kullanım güncellendi. `rgba(13,78,151,0.12)` (`.bt-win-*` demo panelindeki farklı amaçlı, daha soluk ring) kasıtlı olarak dokunulmadan bırakıldı. `pages-mobile.js`'te bu renklerle eşleşen kullanım yok, dokunulmadı.

- **2026-07-24 (devam 6)**: Bir önceki notta Textarea'nın error ring'inin "her zaman görünür" olması gerektiği yazılmıştı — kullanıcı bunun Figma'da kendi hatası olduğunu belirtti, doğrusu diğer tüm input'larla (TextBox/Dropdown/MultiSelect/DatePicker/SelectLookUp — hepsi `.bt-tbx` paylaşıyor) aynı: ring **sadece focus'ta** görünmeli. `.bt-txa` bu davranışa geri döndürüldü (plain Error = sadece border, Error Focused = border+ring), sadece rgba değeri düzeltildi. Asıl kalıcı bug `.bt-tbx`'te (ve onu paylaşan Dropdown/MultiSelect/DatePicker/SelectLookUp'ta) de bulundu — hepsi yanlış `rgba(179,29,56,0.5)` kullanıyordu, doğrusu `rgba(232,75,91,0.24)` (Figma "Focus Ring/error"). Hem CSS hem `pages-web.js`'teki tüm bu component'lerin dokümantasyon tabloları/kod panelleri düzeltildi. Ayrıca Textarea'nın **Filled** state'inde eksik olan Clear Control (× butonu, sağ üstte absolute, `.bt-tbx__clear`+`.bt-txa__clear`) eklendi — hem CSS'e hem preview/code/css panellerine hem Class Reference tablosuna işlendi. `txaCss` kod panelindeki eski (boyuta göre değişen, yanlış) padding hesaplaması da sabit `--bt-space-sm` (6px) olarak düzeltildi.

- **2026-07-24 (devam 5)**: **Textarea** component'i Figma'daki "Base TextArea" (574:2591) ve "TextArea" (579:25815, 3 boyut × 9 state) node'larıyla yeniden karşılaştırıldı, 2 gerçek hata bulunup düzeltildi: (1) padding her boyutta (Sm/Md/Lg) Figma'da sabit `--bt-space-sm` (6px, dört yön) iken kodumuzda boyuta göre yanlışlıkla değişiyordu (4/6/8px dikey, 12px yatay) — `.bt-txa__text` tek bir sabit `padding: var(--bt-space-sm, 6px)` kuralına indirgendi, boyut kuralları sadece `min-height` bırakacak şekilde sadeleştirildi. (2) Error ring rengi/alfası yanlıştı (`rgba(179,29,56,0.5)` yerine Figma'nın "Focus Ring/error" token değeri `rgba(232,75,91,0.24)` olmalıydı) ve sadece focus'ta değil **her zaman** (plain Error state'te de) görünmesi gerekiyordu — `.bt-txa--error`/`--error-focused` tek kurala birleştirildi. `pages-web.js`'teki Textarea "CSS Properties" tablosu da bu doğru değerlere güncellendi (diğer component'lerin aynı desendeki eski tabloları — TextBox/MultiSelect/Dropdown/DatePicker vb. — kasıtlı olarak dokunulmadan bırakıldı, onların CSS'i ayrıca doğrulanmadı).

- **2026-07-24 (devam 4)**: Figma'nın Effect Styles panelinde "xs, sm, md, lg, xl, 2xl, 3xl" olmak üzere 7 shadow (elevation) seviyesi olduğu bulundu (REST API token'ı süresi dolduğu için `figma_get_variables` ile değil, kullanıcının bir katmana geçici olarak uygulayıp seçmesiyle `get_variable_defs`'ten okundu). `styles.css`'e `--bt-shadow-{xs|sm|md|lg|xl|2xl|3xl}` olarak eklendi; projede zaten literal rgba olarak kullanılan **md** (`.bt-switch__thumb`, `.bt-win-dropdown__list`) ve **lg** (`.bt-dd-options`, `.bt-cal`, `.platform-dropdown`) bu token'lara geçirildi — `.platform-dropdown`'daki hafif yuvarlama farkı (`0.03/0.08` → `0.031/0.078`) da bu sırada düzeltildi. "Our Tokens" sayfasına Radius'tan hemen sonra yeni bir "Shadow" bölümü eklendi (TOC'a da işlendi). design.md §3.7b'ye taşındı.

- **2026-07-24 (devam 3)**: "Text Field" component'i **"Textarea"** olarak yeniden adlandırıldı (`pages-web.js`: nav label, sayfa route'u `components/text-field` → `components/textarea`, başlık, prose metinleri) — CSS class prefix zaten `.bt-txa` (textarea) olduğu için isim artık koddaki gerçeklikle örtüşüyor. `pages-mobile.js`'teki nav girdisi (aynı isim, gerçek sayfa implementasyonu yok, placeholder'a düşüyor) kullanıcının isteğiyle dokunulmadan bırakıldı.

- **2026-07-24 (devam 2)**: `pages-web.js`'te Anatomy/States alt-başlıklarında (`Standart Sidebar`, `Hub Sidebar` etiketleri + Button/SplitButton "States" theme başlıkları) gerçek bir bug bulundu: `color:var(--bt-text-primary)` — bu isimde bir CSS değişkeni hiç yok (sadece `--bt-text-primary-default/-muted/...` gibi soneksiz olanlar var), fallback da olmadığından sessizce yanlış renge düşüyordu. `--bt-text-primary-muted` + fallback'e düzeltildi. Aynı elementlerdeki hardcoded `margin-bottom:8px` / `margin:20px 0 12px` de `var(--bt-space-md/-3xl/-xl, ...)` token'larına çevrildi. `font-size:13px` kasıtlı literal bırakıldı — skalada tam karşılığı yok (Figma'nın kendi ayrı `text-1`/13px primitive'i, bkz. `.bt-dd-option__text`'teki aynı durum).

- **2026-07-17**: Checkbox / Radio Button / Switch component'lerindeki hardcoded değerler retroaktif olarak `--bt-*` token'larına geçirildi.
- **2026-07-20**: Isolation mode tüm playground'lar için standart hale getirildi; `pgd_id` URL parametresi sistemi eklendi. Repo adı `MobileDesignSystem` → `Bentas-Design-System` olarak değişti.
- **2026-07-22**: Sidebar component'i **Standart Sidebar** (`.stb-*`, tek panel 280↔48px) ve **Hub Sidebar** (`.sbx-*`, kalıcı rail + drawer) olarak iki ayrı tipe ayrıldı; ikisi de Figma'dan tekrar tekrar doğrulanarak (padding/gap/renk/state hataları düzeltildi) yeniden inşa edildi — hover/active/selected/focus state'leri gerçek çalışıyor, searchbox gerçek SearchBox component'ini reuse ediyor. `--bt-surface-secondary-intense` eksik token'ı eklendi, `--bt-surface-primary-intense`'i ezen hatalı ikinci tanım kaldırıldı. Isolation mode Sidebar için özel "app gibi" tam ekran görünüm aldı. Standart Sidebar, Medusa Dashboard projesine (`medusa-demo/dashboard.html`) de taşındı — bkz. design.md §6.
- **2026-07-24**: Figma'nın Local Variable Collections'ı (`figma-console` MCP, `figma_get_variables`) ile "Our Tokens" sayfası karşılaştırıldı — **Semantic Typography** koleksiyonu (112 değişken: Text/Title/Label/Subtitle × Regular/Medium/SemiBold/Bold) hiç dokümante edilmemiş bulundu. `--bt-{family}-{size}-{weight}` deseniyle (örn. `--bt-title-lg-semibold`) `styles.css`'e composite `font` shorthand token olarak eklendi, "Our Tokens" sayfasına yeni bölüm olarak işlendi. Ayrıca `--bt-brand-*` semantik renk grubunun Figma'da karşılığı olmadığı (muhtemelen Primary'nin elle eklenmiş kopyası) tespit edildi — henüz aksiyon alınmadı.
- **2026-07-24 (devam)**: Eski ayrık `--bt-text-{size}-size` / `--bt-text-{size}-lh` scale token'ları **tamamen kaldırıldı** — artık tek kaynak composite typography token'ları (`--bt-text-xs-regular` vb.), 112 tanesinin hepsine literal px değerleri gömüldü. Bu token'ları kullanan ~30 CSS kuralı (Checkbox/Radio/Switch label-required-desc, SearchBox/TextBox/TextArea/MultiSelect/Dropdown/DatePicker/Upload/Sidebar) tek tek `font: var(--bt-text-xs-regular, ...)` composite kalıbına geçirildi; `.stb-title` özel olarak `--bt-title-lg-medium`'a bağlandı (Figma yorumundaki "Title/lg/Medium" referansına göre — Text değil Title ailesi). 3 yerde (`Checkbox/Radio/Switch __label-row` `min-height`) ve `.bt-dd-option__text` (13px, skalada karşılığı olmayan boyut) karşılığı kalmayan `line-height` kullanımları literal px'e çevrildi. "Our Tokens" sayfasında "Typography" ve "Semantic Typography" tek bölümde birleşti (`Typography`), TOC'a Text/Title/Label/Subtitle alt-başlıkları eklendi (`app.js` `renderToc` nested item desteği kazandı, `.toc-link--sub` stili eklendi). design.md §3.8 tek bölümde birleştirildi, Sidebar/Alert/Upload örnek CSS'leri güncellendi. `pages-web.js`'teki "CSS Properties" referans tabloları (Sidebar/SearchBox/Dropdown/MultiSelect/DatePicker/TextField/Upload — salt metin, `tk()` ile gösteriliyor, canlı render etkilenmiyordu) da yeni composite token adlarına güncellendi. **`pages-mobile.js` bilinçli olarak dokunulmadan bırakıldı** — kullanıcı o dosyayı ayrı bir oturumda baştan değiştirecek, o yüzden içindeki ~69 eski `--bt-text-{size}-size/-lh` referansı (Alert/Avatar/Badge/BottomSheet/Dialog/Card/Chip/Login/Toast önizlemeleri, çoğu fallback'siz inline `style=`) hâlâ kaldırılmış token'lara işaret ediyor ve bir sonraki pages-mobile.js oturumunda düzeltilmesi gerekiyor.
- **2026-07-23**: **Upload** component'i eklendi (`.bt-dropzone`, `.bt-upload-file`, `.bt-upload`). Figma Desktop Bridge ile 5 bileşen incelendi. Tamamen interaktif: `btUplStartUpload/CompleteRow/Remove/Retry/Drop/SetDzState` global fonksiyonları. `Select Files` butonu `bt-btn bt-btn--xs bt-btn--primary-ghost` (design system button reuse kuralı bununla standartlaştı). Playground CSS sekmesi standartlaştı (`esc + <pre class="code-block">` return formatı). Playground zemin + isolation mode background `--bt-surface-subtle` → `--bt-surface-light` düzeltildi. design.md §12 + CLAUDE.md güncellendi.
